#include<stdio.h>
#include<math.h>
int main()
{
int a,b,c,d;
scanf("%d%d%d",&a,&b,&c)
printf("the coefficient of the the equation are %d,%d,%d.\n")
d=b*b-4*a*c;
if(d is less than 0)
printf("the roots are complex\n.")
else (d is greater than 0 )
    printf("the roots are real\n.")
else(d ie equal to 0)
printf("the roots are equal\n.)
return 0
}
