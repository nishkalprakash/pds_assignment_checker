#include<stdio.h>
#include<math.h>

int main()
{
float a,b,c,d;
float x1,x2,x;
      a(x*x)+bx+c=0
      d=b*b-4ac
      printf("the coefficient of the equations are %f,%f,%f\n",a,b,c)
      printf("enter the value of %f:\n",a);
  scanf("%f",&a)
   printf("enter the value of %f:\n",b);
  scanf("%f",&b)
   printf("enter the value of %f\n",c);
  scanf("%f",&c)
  if (a==0&&b==0)
  printf("there is no solution of quadratic equation");
  else if(a==0)
  {
  x=-c/b
  printf("there is only one root of equation, x=-%f/%f\n",c,b);
  }
  else if (d<0)
  printf("the roots of the equation are imaginary\n");
  else
  {
xl= (-b+sqrt(d))/(2*a); x2= (-b-sqrt(d))/(2*a); printf("The roots are real"); printf("x1=%f \n x2=%f",x1,x2);
}







}
