#include <stdio.h>
int main()
{
    int a,b,c;
    scanf(''%d%d%d'',&a,b,c);
    printf(''the coefficients of the equations are a,b,c read is %d/a,b,c'',a,b,c);
    if roots are complex printf(''so, nothing else to be done'')
    if p and q are the roots of the equation printf(''the roots of the equation are p and q'')
    printf(''the roots are equal'')
}
