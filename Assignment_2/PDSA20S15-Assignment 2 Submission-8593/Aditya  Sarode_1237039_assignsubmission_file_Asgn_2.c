
// Online C compiler to run C program online
#include <stdio.h>
#include <math.h>

int main() 
{
    int a , b, c;
    scanf("%d%d%d", &a, &b ,&c);
    printf("The coefficients of the equation are %d , %d , %d \n ", a , b, c);
    {
      int D,B;
      double s;
      s= sqrt(x);
      x= pow(b,2)-4*a*c ;
      D= (-1*b)+ sqrt(x)/2*a;
      B= (-1*b)-sqrt(x)/2*a;
if(D=0&&B=0)printf("The roots are equal \n");
if(D>0&&B>0) printf("The roots are %d and *d \n", D ,B);
else 
printf("The roots are complex \n");

}
