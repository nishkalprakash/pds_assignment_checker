#include <stdio.h>
int main()
{
    int a, b ;
    scanf(''%d%d'',&a,b);
    scanf(''%d%d'',&c,d);
    printf(the value of (a,b) (a,y) (c,d) (x,b) is read as %d/a,b'',a,b);
    scanf(the (p,q)lies inside a reactangle or outside the reactangle)
    ''the point (p,q)is inside the reactangle '' or ''the point (m,n)is not inside the reactangle''
}
