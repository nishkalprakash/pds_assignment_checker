#include <stdio.h>
#include <math.h>

void main()
{
   int a,b,c,d,e,f,i,j;
   printf("Enter the coordinates of the bottom left corner the rectangle:\n");
   scanf("%d%d",&a,&b);
   int c,d;
   printf("The coordinates of the upper right corner of the rectangle:\n");
   scanf("%d%d",&c,&d);
   printf("The coortinates of upper right corner of rectangle are (%d,%d)\n");
   int e,f,g,h;
   printf("The coordintes of the rectangle are (%d,%d),(%d,%d),(%d,%d),(%d,%d)\n");
  int i, j;
  printf("Enter coordinates of two piont to be checked:\n");
  scanf("%d%d\n,&i,&j");
  if (a*<i<i<b*j);
  printf("The point lies inside the rectangle");
  else
    printf("The point lise outside the rectangle");













}
