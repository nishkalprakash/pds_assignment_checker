#include <stdio.h>
#include <math.h>
int main()
{
   float a,b,c,d,x1,x2,y1,y2,p,q;
   scanf("%d%d",a=2,b=1);
   scanf("%d%d",c=7,d=5);
   var("x1-y1-x2-y2");
   printf("The four corners of the rectangle are (a,b),(x2,y2),(c,d),(x1,y1)");
   if
    (p>a && p<c) and (q>b && q<d);
     printf("The point (p,q) is inside the rectangle\n");
   else
     printf("The point (p,q) is not inside the rectangle\n");
   return0
}
