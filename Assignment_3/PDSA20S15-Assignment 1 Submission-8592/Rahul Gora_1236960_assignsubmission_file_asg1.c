#include<stdio.h>

void main()
{int a,b,c,d;
printf("enter bottom-left corner,s cor:");
scanf("%d,%d",&a&b);
printf("enter top right corner`s cor:");
scanf("%d%d",&c&d);
inte,f,g,h;
//e f for bottom-right
e=c;
f=b;
//g h for top left
g=a;
h=d;
printf("the four corndinates of new point:");
 scanf("%d%d,&p&q");
 if(p>a&&P<c){if (q>b&&q<d){
 printf("the point (%d%d) in inside the rectangle\n",p,q);
 }else{
 printf("the point(%d%d) in not inside the rectangle\n");

  }
  }
 else {
     printf("the point (%d,%d) in not inside the rectangle\n");

 }
}
