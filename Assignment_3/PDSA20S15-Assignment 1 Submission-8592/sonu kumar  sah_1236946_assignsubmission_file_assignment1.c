#include <stdio.h>
#include <math.h>

int main()
{
    double a,b,c,d,x,y,z,t,p,q;
    scanf(%d%d,&a,&b);
    scanf(%d%d,&c,&B);

    //condition to find the other coordinates if two of them are given
    {
    x = a+c-z
    y = b+d-t
    z = a+c-z
    t = b+d-y
    printf("The four corners of the rectangle are (a,b),(c,d),(x,y),(z,t)");
    }
    scanf(%d%d,&p,&q);

    //condtion to check whether that point (p,q) lies inside the rectangle made by (a,b),(c,d),(x,y),(z,t)






