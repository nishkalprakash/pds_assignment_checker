-1;Did not submit Intermediate Code
1;MaxOverlap():Putting in and returning through proper parameters (must pass a pointer)
1;MaxOverlap():Finding count of number of overlaps for intervals
1;MaxOverlap():Keeping track of max
-1;MaxOverlap():Used additional array
-1;MaxOverlap():Changed return type from void
0.5;DeleteInterval():Passing a pointer to the INTERVALSET structure
3;DeleteInterval():Correctly shifting the intervals around
0.5;DeleteInterval():Handling the case when L and R may have to be set to NULL (no non-overlapping interval)
-2;DeleteInterval():Used extra array
-1;DeleteInterval():Changed return type from void
1;main():Proper malloc of the L and R arrays