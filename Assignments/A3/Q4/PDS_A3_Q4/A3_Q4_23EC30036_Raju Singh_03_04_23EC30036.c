#include<stido.h>
int main ()
{
  int a,b,c,d,x,y;
  printf("Enter the coordinates of the bottom left corner of rectangle ");
  scanf("%d%d",&a,&b);
  printf("Enter the coordinates of the top right corner of rectangle ");
  scanf("%d%d",&c,&d);
  if ((c<=a)||(d<=b))
    printf("till formed rectangle");
  return 0:
}
 else
   {
     printf("enter coordinates of a point p(x,y)");
     scanf("%d%d",&x,&y);
     if((
 
