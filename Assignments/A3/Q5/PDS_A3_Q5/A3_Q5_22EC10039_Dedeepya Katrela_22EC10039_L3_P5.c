#include<stdio.h>
int main()
{
  int date,month;
  printf("enter date and month in the form of numbers");
  scanf("%d %d",&date,&month);
  if ((date<31)&&(month<12))
{  printf("valid date");}
  else 
  printf("invalid date");









 return 0;
 }
 
  
