#include <stdio.h>
int main()
{
int a,b,checkbalance,exit,balnce=0;
if(a<b, printf("withdraw failed balance insufficient"));
else(printf("balance left=a-b"));
printf("Deposite",a);
printf("withdraw",b);
return 0;
}
