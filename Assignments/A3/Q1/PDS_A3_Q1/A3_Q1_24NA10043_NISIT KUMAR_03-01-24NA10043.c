#include<stdio.h>

int main(){
	int day, month, year;
	
	printf ("write date in  DD-MM-YYYY ");
	scanf (%d %d %d, &day,&month,&year);
	if (day<=31,& month<=12 )
	
		printf("VALID DATE")
	if (day=29,& month= 2,& year=2020);
	printf( "INVALID DATE")
	
