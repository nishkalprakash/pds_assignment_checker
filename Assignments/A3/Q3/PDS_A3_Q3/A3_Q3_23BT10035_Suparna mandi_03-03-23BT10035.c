//ROLL NO.:23BT10035
// NAME.:SUPARNA MANDI

#include<stdio.h>
int main()
{
  int x,y;
  printf("enter the coordinates of point ");
  scanf("%d%d" ,&x,&y);
    if(x==0)
      printf("point is in y-AXIS");
    else if (y==0)
      printf("point is in y AXIST");
    else
      {
	if (x>0&&y>0)
	  printf("point is in is quad");

	if (x>0&&y<0);
      }}
	  
