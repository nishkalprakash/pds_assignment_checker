#include<stdio.h>  		//this is an standerd input output header file.
#include<stdlib.h>		//this is an standerd library header file.
#include<string.h>		//this is an string header file.
#include<math.h>		//this is an maths header file.
int main{				

typedef struct  set{		// this is an single linked list structure to store a list of character strings.
char data;
set *next;
}
struct list{				//defining linked list l1 and l2.
	int l1,l2;
	scanf("%s%s",&l1 &l2);
	struct list *next;
}
int l;
int l=l1-l2;
printf("%d",l);			//obtaining the diffrence of l1 and l2.
return 0;
}
