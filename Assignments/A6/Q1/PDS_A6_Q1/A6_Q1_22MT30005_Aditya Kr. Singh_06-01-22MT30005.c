#include<stdio.h>
#include<time.h>
#define pi=3.1415926535
    long int fact(int n)
{
    if(n==1) return(1);
    else return(n*fact(n-1));
}

float pow(floatx, int n)
{
    if(n==1)return(1);
    else
        return (x*power(x,n-1));
}

i_sin(double x)
{


}





















