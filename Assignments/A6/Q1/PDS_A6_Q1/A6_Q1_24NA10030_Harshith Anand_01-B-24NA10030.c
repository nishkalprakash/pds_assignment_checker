// LAB TEST 1
// SET B
// ROLLno 24NA10030
// terminal 30


#include<stdio.h>
#include<stdlib.h>
#include<time.h>
int main()
{
   float Filter[N];
   int Bin_A[A];
   int Bin_B[B];
   int x,i,j,k,A,B;
   float y,sum=0;
   srand();
   int randomNumber = rand() % 100 + 1;
   
   
   
  
   
   y= (k*k/2*k+1) - (((-1)**k)*k) + sin(x/3);
   for(x<=100)
   {  
      for(i=0;i<=N;i++);
        {
           for(k=1;k<=x;k++)               
              { 
                 sum = sum + y;
                 Filter[i] = sum;
              }
                for(j=1;j<=A;j++)
                  {
                      for(k=1;k<=B;k++)
              
              {
                  if(y<Filter[i])
                  x=Bin_A[j];
                  else(y>Filter[i])
                  x=Bin_B[k];
              }
              printf("Random Number: %d\n",randomNumber);  
              }
             } 
             return 0;
             }
             }
              
        
                   
                 
              
        
   
   
   
      
    
                 
                 
                   
            
        
     
     
