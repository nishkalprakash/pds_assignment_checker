#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#include<math.h>

int main() {
	int a,int b,int N;
	int BIN_[a],BIN_[b];
	int x,y,k,i,n;
	float FILTER[n];
	//compute the value of y given x betweet 1 to 100 //
	// where k=(1 to 100)//
	printf("Enter a number x: %d",x);
	scanf("%d\n",&x);
	//(y==(((-1)^(k+1)(k^2)/(2k+1))  + cos(x/2),x)//
	scanf("&d\n");
	// from here we get y//
	
	for(i=0;i<=n;i++) {
	if(n=0;n<=100 && n>0;n++)
	printf("Store in bin a and bin b ;");
	
	}
	//Now if the value of y >x we store it in BIN A//
	//if the value od y <x we store it in BIN B//
	if(y=0;y<x;y++) {
	printf("Store in BIN_A[a]:");
	scanf("%d\n",&BIN_[a]);
	}
	//these value stored in BIN_A//
	else{
	printf("Store in BIN_B[b] :");
	scanf("%d\n",&BIN_[b]);
	}
	//these value stored in BIN_B//
	//we have to do this until BIN_A and BIN_B get full//
	return 0;
	}
