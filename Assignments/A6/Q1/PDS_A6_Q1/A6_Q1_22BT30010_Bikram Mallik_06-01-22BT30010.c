#include<stdio.h>
int fact(int n);
float pow(float x, int n)
double i_sin(double x)
int main()
{   double s;
    printf("enter the angle",);
    scanf("%lf",&s);

    return 0;
}
int fact(int n)
{
    if(n==0)
        return 1;
    else return n*fact(n-1);
}
float pow(float x, int n)
{
    if (n==0)
        return 1;
    if (n=0)
        return x*pow(x,n-1);
}
double i_sin(double x)
{
    double sinx=0,x=1;
    int n=0
    while(n=0)

}
