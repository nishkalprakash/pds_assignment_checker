#include <stdio.h>
int arr(int x[]);
main()
{
  int a[],i,n;
  for(i=0;i<n;i++)
    scanf("%d\n",&a[i]);
}
int arr(int k[], size_t)
{
  int i,n,exc[];
  for(i=0;i<n;i++)
    printf("%d\n",a[i]);
  a[i]=a[i+n/2]
    printf("%d\n",exc[]);
  if(i=n)
    {
      return a[0]=a[n];
    }
  else
    {
      return a[i+1]=a[i];
    }
  if(i=0)
    {
      return a[n]=a[0];
    }
  else
    {
      return a[i]=a[i+1];
    }
  int even[],odd[];
  {
    (n%2==1)
      for(i=0;i<n/2;i++)
	printf("%d",odd[]);
    (n%2==0)
      for(i=n/2;i<n,i++)
	printf("%d",even[]);
  }
  int a[];
  {
    for(i=0;i<n;i++)
    a[i]=a[i+n/2]
      printf("%d",a[]);
  }
  int x,a[],i,n;
  for(i=0;i<n;i++)
    printf("%d",a[]);
  scanf("%d",&a[]);
  if x=a[i]
	 {
	  printf("the index of x is %d",i);
	 }
  else
    {
     printf ("not found");
  return 0;
}
  
