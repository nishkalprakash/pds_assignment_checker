#include int main() { char str[100]; printf("enter string : "); gets (str); printf("the string is %s", str); return 0; }#include #include #include clock_t start, end; double cpu_time_used; /* start = clock(); ... /* part of the program whose CPU time is calculated */ /* end = clock(); CPU_time_used = ((double) (end-start)) / CLOCKS_PER_SEC;*/ float power int main() { float x,p; int n; printf("enter x : "); scanf("%f", &x); printf("enter n: "); scanf("%d", &n); p=power(x,n); print("the value of pow(x,n) is %d/n",pow(x,n)); return 0; } float pow(float x, int n) { if(n=1); { return x; } return 0; } }