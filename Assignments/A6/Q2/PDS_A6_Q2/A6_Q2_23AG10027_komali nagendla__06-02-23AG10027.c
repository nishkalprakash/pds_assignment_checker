//roll no.:<23AG10027>
//name:<komali>
#include<stdio.h>
int a[30],i,n,sum,max,rev[30];

  if(a[0]=0)
    {
      return 0;
    }
  else
    {
      return sum=sum+a[i];    }
  if(a[0]=0)
    {
      return 0;
    }
  if(a[i]>max)
    {
      return max=a[i];
    }
  if(i=n/2)
    {
      return a[i]=a[n/2];
 }
  else
    {
      return a[i]=a[n-i-1];
 }

  int main()
    int a[30],i,sum=0,max=0 ,rev[30],n;
  for(i=0;i<30;i++)
    {
      printf("%d\n",rand()%10+20);
      sacnf("%d",&a[i]);
      printf("enter sum",sum);
      printf("enter max is %d",max);
      for(i=0;i<30;i++)
 {
   printf("enter the rev[]",rev[30]);
 }
      return 0;
    }