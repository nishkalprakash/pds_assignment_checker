//NAME : HARSH PATIDAR
// ROLL NO . : 22MF10015
// SECTION : 2
// PROBLEM 3
//DESCRIPTION ;

#include<stdio.h>

int main()
{
    int ch, len;
    printf("h");
    scanf("%d",&ch);
    switch(ch){

    case 1 :
    printf("enter the length of the array");
    scanf("%d",&len);
    int ar[100];
    printf("enter array element");
    for(int i=0;i<len;i++)
    {
        scanf("%d"&a[i]);
        reverse();
        break;
    }
    case 2 :
        printf("enter a string");
        get(a);
        printf("enter another string");
        get(b);
        anagram(a,b);
        break ;
    case 3 :
    printf("enter string");
    get(a);
    int result=palidrome;
    if(result==1)
    {
        printf("%S is not a a palimdrone",a);
        printf("\n");
        break;
        default : printf("wrong choice");

    }
            }
            v

}
void reverse(int [a], int[n])
int x=n-1,i,cons;

for(i=0;i<l;i--)
{
    cons=a[i];
    a[i]=a[x];
    a[x]=cons;

}
for(i=0;i<n;i++){
    printf("a[i]");
    printf("\n");
    printf(reverse order of the array is);

    for(i=0;i<n;i++){
        printf("a[i]");
        void anargram(char a[],char b[]){}
    }
