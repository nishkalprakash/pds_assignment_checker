#include<stdio.h>
#include<string.h>
int main()
{
     int num;
int test_cases ;
printf("enter the no of elements %d",num);
scanf("%d",&num);
int arr[100];
int p;
for(int i=0;i<num;i++);
{
    scanf("%d",&p);
    arr[i]=p;
}
int palindrom (char[20]);
char str = [*aba*]
int l=0
int h = strlen (str)-1;
while (h>l){
    if ( str [ l++]! = str[h--])
        printf ("%s is not a palindrom");
    return 0;
}
printf("%s is a palindrom");
}
return 0;
