#include<stdio.h>
int main()
{
  int i,sum=0;
  int rollno[10],float CGPA[10];
  float j,avg;
  for(i=0;i<10;i++)
    printf("enter the rollno is:%d",rollno[i]);
  for(j=0;j<10;j++)
    printf("enter the CGPA is:%f",CGPA[j]);
  sum=sum+CGPA[j]
    avg=sum/10
    printf("the avg is:%f",avg);
  return 0;
}
