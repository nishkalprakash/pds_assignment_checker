/* NAME:VADITHYA DUSVANTH
   ROLL NO:22PH10051
   SECTION:14
   TOPIC:SORTING AND SEARCHING */
#include<stdio.h>

int main()

{ int max,n,j,i,a,max1,temp,maxs,index;
 for (int i=1;i<n;i++)
       {
          max=i;
               } int arr[max];
                 for(int j=i+1;j<n-1)
                    if(arr[max]<arr[j])
                      {
                       max=j;
                          }
                           if(max1=i)
                            {
                              temp = arr[i];
                              arr[i] = arr[max];
                              arr[max] = temp; 

        }
{
  int n;
  printf("enetr the number of inputs: ");
  scanf("%d",&n);
  
  int arr[n];
  printf("enetr the the numbers");
  for (int i=0;i<n;i++){
             scanf("%d",&arr[i]);
               }
   maxs =arr[0];
   int maxsindex=0;
    for(int i = 0;i<n;i++;)
    {
     if(maxs<arr[i];
        arr[i]/10>0 );}
     {
       maxs = arr = i;
       }
        printf("maxsindex = %d",index);
       int temp = arr[0];
       arr[0]=arr[maxsindex];
       arr[maxsindex]=temp;  

  return 0;
}

   
