#include <stdio.h>
#include <stdlib.h>

int main()
{
    char arr[n];
    int n, i;
    char *p = arr;
    printf("Enter the number of names you want sorted\n");
    scanf("%d", &n);

    for(i = 0; i < n; i++)
    {
        scanf("%s", &arr[N]);
    }

    for(i = 0; i < n; i++)
    {
        printf("%d" arr[N]);
    }
    return 0;


}
