#include<studio.h>
#include<string.h>

int main()
{
    int n,p;
    printf("Enter the value of names in first array :\n");
    scanf("%d",&n);
    p = n+1;

    char=X;
    X=(char*)malloc(p*sixeof(char));

    for (int i=0; i<p ; i++)
    {
        scanf("%f", &s);

    }
    int n,q;

    printf("Enter the names in second array :\n");
    scanf("%d",&n);
    q=m+1
    char *Y;
    Y =(char*)malloc(q*sizeof(char));

    for (int j=0 , j<q ; j++)
    {
        scanf("%s",&Y[j]);
    }
    printf("printing %d names in X :\n",n);
    for (int i=0, i<n ; i++)

    return o;
}
