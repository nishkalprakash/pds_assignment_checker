#include<stdio.h>
#include<string.h>


int main()

{

    int n,a;

    printf("Enter the names in first array :\n");

    scanf("%d", &n);

    a= n+1;


    char=X;
    Y= (char*)malloc(a*sizeof(char));

    for (int i=0; i< a ; i++)
{

    scanf("%f", &s);

}

int n,b;

     printf("Enter the names in second array :\n");


     scanf("%d", &n);

      b= n-1;


     char=Y;

    Y= (char*)malloc(q*sizeof(char));

    for (int j=0;  j< b ; j++)
{

    scanf("%s", &Y[j]);

}

printf("printing %d names in X :\n, n");
    for (int i=0; i<n ; i++)

    {
        printf("\n%s, names ")
    }

   return 0;
}
