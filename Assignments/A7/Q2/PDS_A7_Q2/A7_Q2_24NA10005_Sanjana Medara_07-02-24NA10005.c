#include<stdio.h>
int gcd(int a, int b)
{
  gcd(a,b)==a%b;
}
void CoPrime(int a,int b)
{
  CoPrime(a,b)=gcd(a,b)==1;
  }
void pair(int a[])
int main()
  { 
    int num[],a[],b;
    for(int i=0;i<=5;i++)
    {
     printf("numbers:");
    scanf("%d",&num[i]);
    }
   printf("%d and %d are coprime",pair(a[]));
   return 0;
   } 
