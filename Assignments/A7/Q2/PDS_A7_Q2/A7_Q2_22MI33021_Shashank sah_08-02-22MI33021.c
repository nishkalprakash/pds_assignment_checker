/* name : shashank sah
sec 2 22mi33021
question no : 2
assignment no : 7 */

#include<stdio.h>
int main()
{
    // declare a pointer ptr to point a 2D matrix of int
    int **ptr;

    //to allocate memory for a 2D matrix of order m cross n
    ptr = (int **) calloc(m, size of (int*));
    for (int i = 0; i < m; i++ )
        ptr[i] = (int *) calloc(n, size of (int));


















    return 0;
}


