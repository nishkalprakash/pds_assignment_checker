return 0;
}

{
    int arr[m][n];

    int i,j;
    int**ptr;
    ptr=(int**)calloc(3,sizeof(int*));
    printf("\n enter the m,n values:");
    scanf("%d%d",&m,&n);

    printf("\n enter the matrix values(3x3):\n");
    for(i=0;i<m;i++)
    {
        for(j=0;j<n;j++)
        {
            scanf("%d",&arr[i][j]);

        }
    }
        printf("matrix A:\n");
        for(i=0;i<m;i++)
        {
            for(j=0;j<n;j++)
            {
                 printf("%d ",arr[i][j]);
            }#include<stdio.h>
#include<stdlib.h>
#include<time.h>
int main()
#include<stdio.h>
#include<stdlib.h>
#include<time.h>
int main()

{
    int arr[m][n];

    int i,j;
    int**ptr;
    ptr=(int**)calloc(3,sizeof(int*));
    printf("\n enter the m,n values:");
    scanf("%d%d",&m,&n);

    printf("\n enter the matrix values(3x3):\n");
    for(i=0;i<m;i++)
    {
        for(j=0;j<n;j++)
        {
            scanf("%d",&arr[i][j]);

        }
    }
        printf("matrix A:\n");
        for(i=0;i<m;i++)
        {
            for(j=0;j<n;j++)
            {
                 printf("%d ",arr[i][j]);
            }
            printf("\n");
        }

        for(int i=0;i<m;i++)
        ptr[i]=(int*)calloc(3,sizeof(int));
        printf("\n enter p value: ");
        scanf("%d",&p);

    printf("\n enter the matrix values(3x3):\n");

    for(i=0;i<n;i++)
    {
        for(j=0;j<p;j++)
        {
            scanf("%d",&arr[i][j]);

        }
    }
        printf("matrix B:\n");
        for(i=0;i<n;i++)
        {
            for(j=0;j<p;j++)
            {
                 printf("%d ",arr[i][j]);
            }
            printf("\n");
        }
        for(i=0;i<m;i++)
        {
            for(j=0;j<p;j++)
            {
                printf("%d",a[i][j]);
            }

        }






            printf("\n");
        }

        for(int i=0;i<m;i++)
        ptr[i]=(int*)calloc(3,sizeof(int));
        printf("\n enter p value: ");
        scanf("%d",&p);

    printf("\n enter the matrix values(3x3):\n");
    
    for(i=0;i<n;i++)
    {
        for(j=0;j<p;j++)
        {
            scanf("%d",&arr[i][j]);

        }
    }
        printf("matrix B:\n");
        for(i=0;i<n;i++)
        {
            for(j=0;j<p;j++)
            {
                 printf("%d ",arr[i][j]);
            }
            printf("\n");
        }
        for(i=0;i<m;i++)
        {
            for(j=0;j<p;j++)
            {
                printf("%d",a[i][j]);
            }

        }       





      return 0;
}