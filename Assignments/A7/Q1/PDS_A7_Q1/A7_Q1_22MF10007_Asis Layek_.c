Write a program that will insert a new value to an already existing sorted array such that the ordering is maintained. You can assume the following:
  a)      The array is to be maintained in ascending order, for example, 1, 2, 3, … 
b)      An  input array is given where all elements are already in sorted order.
  User Input: Number of elements in the array, say n, n elements in the array in sorted order, the new value to be inserted. 
Output: The input array, the output array