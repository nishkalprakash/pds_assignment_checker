#include<stdio.h>
int power(int x,int n);
scanf("%d%d",&x,&n);
return x;

int fact(int n);
scanf("%d",&n)
return nfact;

int main(){
{
int x;
x=1;
float sum;
sum=1;
int nfact;
nfact=1;
for(int i=1,i<n,i++)
x=x*i;
i=(x-1);

float sum=sum+power(int x,int n)/fact(n);

}
printf("the sum of the first n turm is %f:",sum);
return 0;
}

