#include<stdio.h>
int power(int x, int n)
{
 int term=1;
 for(int i=0;<n;i++){
 sum=sum+term;
 term=
 
//creating function factorial 
int fact(n)
{
 int product=1;
 for(int p=1;p<=n;p++){
 product=product*p
 return product;
 
int main()
{
 int x,n;
 printf("enter the value);
 scanf("%d\n", &x);
 printf("enter the value n = ");
 scanf("%d", %n);
 
 for (int z =1; z<n; z++){
    sum = sum + power(x,z)/(float)fact(x);
}

 printf("sum of %f number of term is %.4f\n", &sum);
 return 0;