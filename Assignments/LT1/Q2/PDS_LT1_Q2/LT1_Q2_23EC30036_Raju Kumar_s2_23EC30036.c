// Raju Kumar
// Roll .no - 23EC30036
#include<stdio.h>
int main ()
{

  int i, rand ;
  while(i<100)
    if((i<=1)&&(i>=10))  /*enter the between 1 to 10 */
      scanf("%d", &rand);
  printf("%d",rand);
  if  ((i<=11)&&(i>=20))
	    printf("%d", rand );
  if ((i<=21)&&(i>=30))
	   printf("%d", rand);
  if  ((i<=31)&&(i>=40))
	    printf("%d", rand);
  if  ((i<=41)&&(i>=50))
	    printf("%d", rand );
  if  ((i<=51)&&(i>=60))
	    printf("%d", rand );
  if  ((i<=61)&&(i>=70))
	    printf("%d", rand );
  if  ((i<=71)&&(i>=80))
	    printf("%d", rand );
  if  (( i<=81)&&(i>=90))
	    prinf("%d", rand );
  else (i>100)
	
	 }
