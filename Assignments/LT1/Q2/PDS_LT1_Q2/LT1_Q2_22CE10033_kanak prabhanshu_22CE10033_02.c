#include<stdio.h>
int main()

{
    int n,s,i;

    printf("Enter the integer : ");
    scanf("%d , &n");

    while(n>=1)
    {
        d= n%10;
        s= (s%10 +d);
        n=n/10

        printf(%d, s);

    }

    printf("Reversed integer : %d\n, s");

    minus = n-s;

    if(minus<0) minus = -1*minus ;
     printf("%d,minus");
     else

        printf("%d\n" , minus);

    if(a%2==0)
    {
        while(a>0){
                a=2
    }

     return 0;
}
