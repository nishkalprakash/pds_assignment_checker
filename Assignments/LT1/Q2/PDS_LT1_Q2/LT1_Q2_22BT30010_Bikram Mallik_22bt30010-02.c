#include<stdio.h>
int main()

{
    int a,b;
    printf("enter the integer:");
    scanf("%d",&a);

    if(a%2=0)
    {
        printf("least significant digits to most significant digits:/n");
        while()


              break
    }
   return 0;
}
