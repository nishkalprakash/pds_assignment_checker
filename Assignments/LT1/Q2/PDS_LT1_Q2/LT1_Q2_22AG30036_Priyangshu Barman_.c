#include<stdio.h>
int main()
{
    int num;
    int d;
    print("Enter The Integer : ");
    scanf("%d",&num);
    while(n > 0)
    {
       d = num / 10;
       num = num % 10;
       printf("%d",num);
    }
    if(num % 2 == 0)
    {
        
    }