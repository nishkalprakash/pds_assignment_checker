#include <stdio.h>
int main()
{
int i,maxi,a,num2;
printf("Enter Numbers\n");
scanf(
if (a<0) {
printf("value not yet Entered\n");
}
else if (a>=0) {
printf("largest number\n");
printf("second largest value : not entered\n");
printf("enter num\n");
do {
  scanf("%d", num2);
  if(num2>max)
  max=num2;
  if((num2<max) && (num2>max))
  max = num2;
  }
  while(num>=0);
  
  }
  return 0;
  }


