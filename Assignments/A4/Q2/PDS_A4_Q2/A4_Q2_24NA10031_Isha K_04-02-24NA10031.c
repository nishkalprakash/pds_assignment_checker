#include<stdio.h>
int main()
{
	int n,max,smax;
	while(n>0){
	printf("Enter numbers:");
	scanf("%d",&n);
	if(n>max){max=n;}
	if(n<max){smax=n;}
	
	printf("Largest number= %d\n",max);
	printf("second largest number:%d\n",smax);
	if(n<0){ printf("<program terminates>");
	 break;
	}}
	 return 0;
	 }
	 
	 
	 
	 
	
