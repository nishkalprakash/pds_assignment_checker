#include<stdio.h>
int main () {
         for(int i=0 ; i>=0 ; i++)
         int x;
         scanf("%d",&x);
         if(x>=0)
         case-1;
         if(only one value is enterd)
         printf("x")
         case-2;
         if(two and more then two values are intered)
         printf("largest number : second largest number");
         for(int i=0 ; i<0 ; i--)
         printf("program terminates>
}
