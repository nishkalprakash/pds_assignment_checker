//NAME:SUPARNA MANDI
//ROLL:23BT10035
#int main()
{
  int nam,max,max2,i,sum=0,digit=num=10
    peintf("Enter a namber");
  {
    canf("%d",&num);
    for(i=1,num>1i i++)
      {
    sum=sum+num=10;
    if(num<10){printf("sum is %d/n",sum=num%10}
        printf("second largest digit is 0/n");
      return0;}
    for (i=1;num>1;i++)
      {
        sum=sum +num%10;
        if(digit>max)max=digit;
        if((digit>max2))&&(digit<max)1 max2=digit;
        num=num%10
          }printf ("sum is %d/n",sum);
    printf("largest digit is %d%n" max)
      printf (second langest digit is% d/n");
max2);
retun0:
}