#include<stdio.h>
int main(){
int number
scanf("%d",&number);
  for(i=1;i<=10;++i)
     rem = number/10^i
   if rem == 1 print "one"
if rem == 2 print "two"
if rem == 3 print "three"
if rem = 4 print "four"
if rem = 5 print "five"
if rem = 6 print "six"
if rem = 7 print "seven"
if rem = 8 print "eight"
if rem = 9 print "nine"
}
return 0;
