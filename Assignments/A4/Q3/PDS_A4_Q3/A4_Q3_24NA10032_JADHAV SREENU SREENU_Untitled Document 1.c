#include<stdio.h>
int main()
{
	int n,term=1,i=0;
	printf("enter the number of term = ");
	scanf("%d", &n);
	printf("the terms are = ");
	 lf (n > 0);
	 
	 while (l <=n)
	 {	("printf("%d, term");
	 term = term = i;
	 i++;
	 }
	 printf("\n");
	while(l <n);
else{
	prinf(" it si invald\n");
}
