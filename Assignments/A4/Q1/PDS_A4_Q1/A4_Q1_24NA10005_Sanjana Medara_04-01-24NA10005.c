#include <stdio.h>
int main()
    {
      int n,j,k=-1;
      scanf("%d",&n);
      printf("n=%d",n);
      for(int i=0;i>=j;i++)
      {
        for(int j=1;j=j-k;j++)
        if(n==0)
        printf("N is invalid");
        else if(n==-n)
        printf("N is invalid");
        else 
        printf("%d",j);
       } 
        return 0;
      
     } 
