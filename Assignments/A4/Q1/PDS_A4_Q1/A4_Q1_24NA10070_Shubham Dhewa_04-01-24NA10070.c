#include <stdio.h>
#include <math.h>
int main()
          {
           int n, T1, T2, Tn, ;
           scanf("%d", &n);
           if(n<=0)
           printf("n is ivalid");
           for(n>0; T1=1; T2=-1);
           {if(n%2==0)
              n=n+1
          
           return 0;
          }
