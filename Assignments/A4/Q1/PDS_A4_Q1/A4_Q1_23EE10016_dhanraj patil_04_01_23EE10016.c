//Roll no: 23EE10016
//Name   : dhanraj patil

#include <stdio.h>
int main(){
  int a,b,c,d,e,f;
    printf ("give total no. of hours taxi was retained");
    scanf ("%d" &a);
 printf ("give total no of kilometers travelled");
    scanf ("%d" &b);
    
    if (a<1 && b<=8) {
      printf ("your total fare is 20 Rs");
      return 0;
	}
    int c=20+10*(b-8);
     if (a<1 && 12>=b>8) {
      printf ("your total fare is %d Rs") &c;
      return 0;
     }
     int d=60+8*(b-12);
      if (a<1 && 16>=b>12) {
      printf ("your total fare is %d Rs" &d);
      return 0;
	}
      int e=92+6*(b-16);
       if (a<1 && 20>=b>16) {
      printf ("your total fare is %d Rs" &e);
      return 0;
	}
       int f=116+5*(b-20);
        if (a<1 && b>20) {
      printf ("your total fare is %d Rs" &f);
      return 0;
	}
	 if (12>a>=1 && b<=8) {
      printf ("your total fare is %d Rs"&);
      return 0;
	}
	  if (a<1 && b<=8) {
      printf ("your total fare is 20 Rs");
      return 0;
	}
	   if (a<1 && b<=8) {
      printf ("your total fare is 20 Rs");
      return 0;
	}
   
    
