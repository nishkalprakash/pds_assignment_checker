//chityala raviteja
//21cs30016
//section 3
#include <stdio.h>
int main(){
int a[10000],i;
printf("enter the number of elements in array of integers\n");
scanf("%d",&n);
printf("enter the elements:");
for (i=0;i<n;i++){
    scanf("%d",&a[i]);
}
printf("enter value of l:");
scanf("%d",&l);
//c is array which contains integers corresponding to indices of elements of array a for each combination
//b is array which is output for each combination
//idea is if element of array c which we are dealing in recursion i.e.,c[i]==l,then previous element of it should be raised by one,and then the dealing element again starts incresing by 1 but starts from value of previous element

void comb(int l,int j){
    b[i]=a[c[i]];
    c[i]++
    if (c[i]=='l'){
        c[i-1]++;
    }
    for (i=0;i<l;i++){
        printf("%d ",b[i]);
    }
    printf("\n");
    }
}
    return 0;
}