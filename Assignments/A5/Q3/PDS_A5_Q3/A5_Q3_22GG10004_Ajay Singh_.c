#include<stdio.h>
#include<time.h>
void guess(int n)
{
int number , guess, number of guess = 1;
srand(time(null));
printf("guess a number between " "1 and %d\n", 100);

do
{
if (number of guess >15)
{
printf("\ntoo big!\n")
break;
}
if(guess > number)
{
printf("small number");
}
else if ( number> guess)
{
printf(