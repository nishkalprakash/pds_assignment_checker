#include<stdio.h>

int main()
{
    int i,a[100],n;
    printf("enter the no of elements to store them in an array:" );
    scanf("%d",&a);
    if(a=0)
    {
        printf("wrong");
    }
    else
    {   for(i=0;i<n;i++)

    }
}
