//Rollno:<23MT30010>
//Name:<Purva Harde>

#include <stdio.h>
#include <stdlib.h>
int main()
{
  int a, b, c, d;
  int arr[50];
  printf("Enter the number of random numbers to be generated \n");
  scanf("%d", &a);

  for(int i = 0; i < c; i++)
    b = rand()%90 + 10;
    arr[a] = b;

  for(int i = 0; i < c; i += 2)
    if(arr[i] < arr[i++])
      {
      int temp = arr[i += 2];
  arr[i += 2] = arr[i];
  arr[i] = temp;
      }
  printf("%ls", &b);
  printf("Interchanges : %d \n", d);
  {
    printf("Invalid \n");
 
}
  return 0;
}
  
  
  
  
  
  
