// LAB5ARRAYS
// Deepak Jangir
// 24NA10023

#include<stdio.h>

int main()
{
	int N, n;
	int array[N];      // N is the size of the array
	 N=100, n<=N;      // n is the number of integer stored in array
	printf("Enter the value of n : ");
	scanf("%d", &n);
	
	for(int i=1; i<=n; i++){
	printf("enter the number : ");
	scanf("%d", &array[i]);
	
	int p, q;
	
	array[0] ==p;
	array[1] ==q;
	
	if(array[i]>0){
	array[0] ==q;
	array[1] ==p;
	}
	}
	
	return 0;		
}
