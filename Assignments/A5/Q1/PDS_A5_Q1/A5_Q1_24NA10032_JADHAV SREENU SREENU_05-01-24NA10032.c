#include<stdio.h>
#define n =100
int main()
{

	int n,i,j, arr[N],temp;
	
	printf("enter the sizeof array (n<N) = ");
	scanf("%d", &n);

	for(int i=0; < n ; i++){
	printf("element-%d:",i);
	scanf("%d",&arr[i]);

	printf("original arr:%d",arr[i]);
	else

	
	for(i=0;<n,n<=N;i++);
	{
			printf("element :%d",i);
	
			scanf("%d", &arr[i]):
	}		
	printf("original array:",);
	for(i=0;i<0;i++);
	printf("%d", arr[i]);
				}
	
	for(j=0;j<i;j++)  {
	
		if(("arr[i]>o&&arr[j],0)) {
		{	
				temp=arr[i];
				temp(arr=[i];
				arr[j]=temp
	
				}
			}
		}
		printf("rearranged array: %\n",);
		for(i=n-1;j>=0;j++) {
		printf("%d",arr[i]);
					}
	printf("\n",);
			}
	
	return 0;	

