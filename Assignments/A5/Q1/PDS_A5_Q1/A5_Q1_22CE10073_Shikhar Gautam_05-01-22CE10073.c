#include <stdio.h>
#define N 100

int main(){

int n; // declaring variable to read n and store them in the array
int arr[]; // declaring our array

printf("Enter the size of the array : "); // taking input of n to get the size of the array
scanf("%d", &n);

if (n > 100){
    printf("Error : n>100");// checking that user input is valid or not
}
else if (n<=100){
    for (int i=0; i<n; i++){
    arr[i] = i
    }

}
i=0;
while (i<n)
{
    printf("Enter elements of the array : ")
    scanf("%d", arr[i])
}

for (int i=0; i<n; i++){
    if (i*arr[i]= -%d){
        printf("Original Array : %d", arr[]);// printing our original array
    }

}












return 0;

}
