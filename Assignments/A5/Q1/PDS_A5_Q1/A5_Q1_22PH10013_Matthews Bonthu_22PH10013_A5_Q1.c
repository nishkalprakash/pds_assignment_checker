/*                                                      
NAME:: BONTHU MATTHEWS 
ROLL NUMBER:: 22PH10013 
ASSIGNMENT 5 
SECTION :: 14 
*/

#include <stdio.h>

int factorial (int n)
{
    int i, temp=1;
    for (i=1; i<=n; i++)
    temp = temp * i;
    return (temp);
} 

int power(int x, int n)
{
  double c;
  c=power(x,n);
  return (c);
}
main ( )
 {
   int i,temp,n,x;
   double r,c;
   printf("enter the values of x and n");
   scanf("%d%d",&x,&n);
   for(r=0;r<=n;++r)
    {
      r= r + ((float)c)/temp;
    }
    printf("%lf",r);
  }

