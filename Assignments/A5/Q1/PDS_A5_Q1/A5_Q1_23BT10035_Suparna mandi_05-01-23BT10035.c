//ROLL NO:23BT10035
//NAME:SUPARNA MANDI
#include<stdio.h>
in main()
{
  int n,i,c,r=0;
    int array[50];
  printf("enter the number of rondom number to be generated");
  scanf("%d",&n);

  for(i=0<n;i++);
	
    {
      array[i]=(rand()%90)+10;
    }
  printf("array content");
  
  for(i=0<n;i<n;i++);
}
 [i+2]=c
      printf("array[i]");
    }
for(i=0;i<(n-2);i=i+2);
    {
      if(array[i]>array[i+2]);
	{
	  c=array[i];
	  array[i]=array[i+2];
	 array[i+2]=c
	   r++;
	}
    }
	 printf("array after interchages");

     for ("r=i=0;i<n;i++");
{
 print("array[i]");
}
 printf("number of intrchages");
