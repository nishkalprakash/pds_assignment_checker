# include<stdio.h>
int main()
    {
       int n;
       int a[n] ={0};
       int i,total;
       printf("enter the number of elements":);
       scanf("%d"&n);
       for (i=0;i<n;++i)
       scanf("%d",&a[i]);
       for(i=0;i<n;++i,total=0)
         {
           if (a[n] == 1)
           break
           a[n] = 1;
           total =+ a[n]
           
         }
           printf("original array:",a[i]);
           printf("Duplicates: \n",a[i] (total));
           return 0;
     }      
            
