/*
Section 14
Roll No : 22CE10005
Name : #Aman Meena#
Assignment No : 5
Description : To find either the two numbers are prime or not*/


#include<stdio.h>


int main()    {

             int gcd(int a, int b),A[20],B[20],i,n,j,count;

             A[20] = void pair(int a[]);

             B[20] = void coprime(int a, int b);



             scanf("%d", & A[i]);

             for(i=0,i<n,i++)
                  scanf("%d", &A[i]);

             for(i=0;i<n;i++)
                  printf("%d", A[i]);

                  printf("\n");

                  count=0;

                  for(i=0;i<n;i++)
                       for(j=0;j<count;j++)

                             if(A[i] == B[j])
                             break;

                 if(j==count)
{
                 B[count] = A[i]

                 count++;
}

}

                for(i=0;i<count;i++)
                printf("%d", B[i]);

                printf("\n");

        return 0;
