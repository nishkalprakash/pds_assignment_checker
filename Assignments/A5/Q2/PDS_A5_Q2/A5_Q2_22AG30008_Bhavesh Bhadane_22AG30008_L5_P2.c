/*
  Section 14
  Roll no : 22AG30008
  Name : Bhavesh Nitin Bhadane 
  Assignment no -5
  Program to check points
*/

#include<stdio.h>
#include<math.h>

int gcd(int a,int b)
{
	int i,j,k;
	for (k=1;k<=sqrt(n);k++)
	{
		if(i%k==0 && j%k==0)
			{printf("GCD IS K");}
	}
	return (k);

}
  void coprime(int a, int b)
  {
  	int i,j,k;
  	for (k=1;k<=sqrt(n);k++)
  		{ if (i%k==0 && j%k==0 && k==1)

  			{ printf("numbers are coprime");

  			}





  		}
  		return (coprime)
  }