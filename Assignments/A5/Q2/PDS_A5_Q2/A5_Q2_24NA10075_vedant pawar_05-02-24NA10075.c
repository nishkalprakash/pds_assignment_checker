#include<stdio.h>
int main()
{
int N;
scanf("%d",&N);
int arr[N];
int count
for (int i=0;i<= N-1;i++)
{
scanf("%d",&arr[i]);
}
for (int i=0;i<= N-1;i++)
{ for (int j=0;j<= N-1;j++ && i!=j)
      
     if(arr[i]==arr[j] ) {count++;}
    { printf("THE DUPLICATES:%d (%d)",arr[i] (count);}
    }
    return 0; }
     
