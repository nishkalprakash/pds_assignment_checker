#include<stdio.h>
#include<stdlib.h>
int main()
{
  int arr[] = ([10,50]);
  int sum=60;
    printf("Enter a numbers:");
    
      sum = 60
	{ printf("These are the triplets that add upto 60, they are %d,%d and %d"sum,sum,sum);
  

  return 0;
}
