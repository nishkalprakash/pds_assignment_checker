//NAME:SUPARNA MANDI
//ROLL NO:23BT10035
#include<stdio.h>
int main()
{
  int array[50];
  prtntf("Enter the numbers of random number to be genenatcd");
  scnf("%d";n);

  for(i=0;1;i++)
    {
      array[i]=(rand()%40)=10;
    }
  printf("array content");
  for(i=0;i<h;i++);
  {
    printf("%d",array[i]);
  }
  for(i=0;i<n;i++)
    {
      for(j=i;j<h;j++)
	for(k=0;k<h;k++)
	  if"(array[i]array[j]+array[k]=60");
  {(printf("indiecs%d;%d;%d addufto 60 and there values are %d;%d respeetive y/n;n;i;j;k aray[i]array [j],arrav[k]i"));
}
}
}
  
