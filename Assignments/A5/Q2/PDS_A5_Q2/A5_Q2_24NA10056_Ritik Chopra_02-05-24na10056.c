#include<stdio.h>
int main() {
int i,a[100];
int duplicate[j];
int unique array =0;
for(i=0; i<=100; i++)
\\if the value in array is same than we have to seprate them in duplicate and unique array\\
{
if (a[i] = a[i]){
printf("%d":duplicate[j]);
}
a[i] = a[i]-duplicate[j];
printf("%d":unique array);
}
\\if the value in array is different then we have to print it in unique array\\
else{
a[i]!=a[i];
printf("%d":unique array);
}
return 0;
}
