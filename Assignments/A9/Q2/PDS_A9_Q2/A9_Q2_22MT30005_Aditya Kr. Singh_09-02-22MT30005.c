#include<stdio.h>

typedef struct vector
{
   int a[100];
} v1,v2;

void readvectors(struct *v1, int n)
{
    for(i=0;i<n;i++)
        scanf("%s", vector[i].v);
}

int main()
{
    int n;
    printf("value of n: ");
    scanf("%s");







return 0;
}

