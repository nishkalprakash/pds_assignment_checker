#include<stdio.h>

struct vector
float vector[n]

          {

  float [n];

}

int (main)

{
    float [n];
    float A,a,B,b,C,c,D,d,E,e;

    printf("Enter dimesion of your vector %d\n", n);
    scanf("%d\n", n);

    vector[o]= A,B,C,D,E;
    vector[1]= a,b,c,d,e;

    if(A*a + B*b + C*c + D*d + E*e ==0)

        {
        printf ("the given vectors are Orthogonal");
    }

    else

        {
        printf ("the given vectors are not Orthogonal");
    }
    return o;
}
