#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main( )
{
struct  vector {
float x;
float y;
} a, b;
scanf ("%f %f", &a.x, &a.y);
scanf ("%f %f", &b.x, &b.y);
    if
    (a.x * b.y = 0);
    printf ("the vectors are orthogonal ",a.x,b.y);
    else if
        (a.x* b.y = 1);
        printf("the vectors are similar",a.x,b.y);
    return 0;

     }