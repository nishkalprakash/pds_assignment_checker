#include<stdio.h>
#define max 100
typedef struct user

{
    char userID(MAX);
    char password (MAX);
}   user;
    user enroll(user*u ,int j)
    {
        user u1;
        printf("enter user id");
        gets(user u1 id);
        printf("enter password");
        gets(u,password);
        return u1;
        
        
    }
    int invalid(user u)
    int i=0;ctr1=0 ,ctr2=0, ctr3=0,ctr4=0;
    while(u.userID[i]!=='\0')
    {
        if(u.password[i]>='A'&& u.password[i]<='z')
        ctr1++;
        if(u.password[i]>='a'&& u.password[i]<='z')
        ctr2++;
        if(u.password[i]>='0'&& u.password[i]<='9')
        ctr3++;
        if(u.password[i]>='-'  || u.password[i]>='!' || u.password[i]>='@' ||u.password[i]>='#'|| u.password[i]>='$' ||u.password[i]>='%' ||u.password[i]>='&' ||u.password[i]>='*');
        if(ctr1=0||ctr2=0||ctr3=0||ctr4=0)
            
    }
{
    printf("invalid password");
    printf("**existing**\n");
    return u1;
    
}
int main()
{
    int n;
    printf("enter maximum number of users");
    scanf("%d, &n");
    getchar();
    user u(n);
    for(int i=0;i<n,i++);
    {
        printf("choose one of the options:\n1.enroll\n\n2.sign in\n3.exit");
        scanf("%d,&n1");
        if(n1==1);
        u[j++]=enroll();
        if(invalid(u))
        if (n1==2)
    
        
    }
    int main()
    {
        int n,n1,j=0;
        printf("enter max numbers of users");
        
    }
    return 0;
}