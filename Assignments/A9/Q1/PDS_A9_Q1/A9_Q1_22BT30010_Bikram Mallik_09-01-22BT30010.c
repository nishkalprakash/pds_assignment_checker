#include<stdio.h>
int fib(int n);
int main()
{
    int n,i;
    printf("enter the value of n>0");
    scanf("%d",&n);
    if(n<0)
        printf("please give possitive no");
        }(

 while(fib(i)<n)
    {
        i++;
    }
    if (fib(i)=n)
        printf("%d is a fibonacci number",n);
    else if((fib(i)-n)<(n-fib(i)))
        printf("nearest fibonacci number is %d",fib(i));

})
int fib(int n)
{
    int a=0,b=1,c,i;
    if(n=1)
      return 0;
    else if(n==i)
        return 0;
    else if(n==2)
        return 2;



}

