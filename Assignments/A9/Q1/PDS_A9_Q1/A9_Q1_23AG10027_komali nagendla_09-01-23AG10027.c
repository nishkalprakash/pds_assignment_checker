#include<stdio.h>
int main()
{
  int student[],grade[],ph.no[];
  int i,j,k,n,size,CGPA;
  for(i=0;i<n;i++)
    printf("enter the size of array is:%d");
  scanf("%d",&size);
  for(j=0;j<n;j++)
    printf("enter the grades of each student");
  CGPA=(Sum of marks*no.of credits)/(sum of credits)
    if (CGPA=10)
      printf("enter the grade E");
    else if(CGPA=9)
      printf("enter the grade A");
    else if(CGPA=8)
      printf("enter the grade B");
    else if(CGPA=7)
      printf("enter the grade C");
    else if(CGPA=6)
      printf("enter the grade D");
    else if(CGPA=5)
      printf("enter the grade p");
    else if(CGPA=0)
      printf("enter the grade F");
  int student[];
  int i,n;
  for(i=1;i<n;i++)
    printf("enter the size of array is:%d");
  printf("enter the names of student");//names should be in alphabetical order//
  if student[i]=a
	      printf("give the element no.1");
  if student[i]=student[i+1]
	      printf("check the second letter");
  int ph.no[];
    int j,n;
    for(j=0;j<n;j++)
      printf("enter the phone number of every student is:%d");
    //details of every student must be nicely formated//

    student[i]=sesh
      for(i=0;i<n;i++)
       if student[i]=sesh
		   printf("enter the value of i is:%d");
       else
	 printf("enter not found");
    if first name is not matched
	       printf("'s','e','s','h','0'");
    if string=('s','e','s','h')
		printf("the string is matched");
    else
      printf("the string is different from 's','e','s','h'");
    int address[];
    int k,n;
    printf("promt the function for entering the roll number ");
    if previous address !=present address
		  for(k=0;k<n;k++)
		  printf("enter the new address is:%c");
laso if previous ph.no!= present ph.no 
for(k=0;k<n;k++)
printf("enter the new phone number is:%d");
"for(i=0;i<n;i++)
printf("enter the detais of every student")
//details of every student after editting//

int roll no[];
int b,n;
for(b=0;b<n;b++)
printf(" promt for the roll number");
printf("enter the roll number");
scanf("%d",&roll number);

for(b=0;b<n;b++)
printf("display the details according to the entered roll number");
GPA=sum of marks in each subject/no.of credits
printf("enter the GPA of every student is:%f");
scanf("%f",&GPA);
printf("enter the details of every student after updating");

for(j=0;j<n;j++)
printf("enter the grade for every student  is:%c");
scanf("%c",&grade[]);
printf("store the data calculate the grades");
printf("display the details of students");
//display the data nicely formatted//

char studarray[];
int c,n;
printf("enter the names of students in the increasing order of their GPA");
if studarray[c]=stud[c+1]
printf("enter according  to the alphabetical order");

printf("display the student details in the merit order");
return 0;
}


		
