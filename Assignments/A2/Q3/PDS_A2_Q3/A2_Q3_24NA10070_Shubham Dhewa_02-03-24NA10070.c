#include <stdio.h>
#include <math.h>

int main ()
{
 int x0, y0, theta, u, a, t ;
 
 scanf("%d, %d, %d, %d, %d, %d", &x0, &y0, &theta, &u, &a, &t) ;
 printf("Starting point is P(%d,%d)", x0, y0) ;
return 0 ;
}
