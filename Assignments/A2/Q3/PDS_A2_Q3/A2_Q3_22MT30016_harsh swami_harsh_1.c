#include<stdio.h>
#include<math.h>

int main()
{
float a, b, c;
float root1, root2;
float rootpart, denom;

rootpart=sqrt(b*b-4*a*c);
denom=2*a;

root1 =(-b+rootpart);
root2=(-b-rootpart);

printf("root=%f/n root2=%f");
return 0;
}
