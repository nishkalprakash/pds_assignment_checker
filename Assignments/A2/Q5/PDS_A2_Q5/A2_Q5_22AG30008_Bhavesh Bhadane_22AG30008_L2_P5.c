#include <stdio.h>

#include <math.h>

double m =0.125 , gr=9.78 ;

printf("Enter the initial height of release of the ball (h)\n") ;

scanf("%f,&h") ;

printf("Enter the number of times it touches the ground(no) to get distance ") ;

scanf("%d ,& no") ;

distance = 
