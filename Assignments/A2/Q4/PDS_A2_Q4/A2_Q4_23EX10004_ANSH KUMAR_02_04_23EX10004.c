// roll no-23EX10004
// NAME-ANSH KUMAR
#include<stdio.h>
int main()
{
  int m,n;
  float a,b;
  printf("value of m");
  scanf("%d",&m);
  printf("value of n");
  scanf("%d",&n);
  printf("the sum m+n is %d\n",m+n);
  printf("the difference m-n is %d\n", m-n);
  printf(" the product m*n is %d\n", m*n);
  printf(" the integer quotient m/n is %d\n",m/n);
  printf(" the integer remainder m and n is %d\n",m%n);
  printf(" the sum of m^2+n^2 is %d\n", m*m+n*n);
  printf(" the square of sum (m+n)^2 is %d\n",(m+n)*(m+n));
  printf("the average (a+b)/2 is %f\n", (a+b)/2);
  printf(" the average of sum of squares (a^2+b^2)/2 is %f\n",((a*a)+(b*b)/2));
  return 0;
}
  
	 
