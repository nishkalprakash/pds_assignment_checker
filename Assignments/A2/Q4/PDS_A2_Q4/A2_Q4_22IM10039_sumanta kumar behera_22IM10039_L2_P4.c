#include<stdio.h>

#include<math.h>

#include<stdlib.h>

int main()

{

   float c,nu,eps

   printf("enter the value of nu\n);  // the value of nu is

   scanf("%d", &nu);

   printf("entr the value of eps\n); // the value of eps is

   scanf("%d", &eps\n);

   c=1/sqrt(eps*nu);

   printf("the speed of the light is %d", c); // the speed of light is

    return 0;

}
