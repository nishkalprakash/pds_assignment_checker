#include<stdio.h>
#include<math.h>

int main()
{
        float a,b;    
        float nun;
        scanf("%f %f",&a,&b);  
        printf("%f %f",a,b);
        nun=1/sqrt(a*b);
        printf("the speed of light is %f",nun);

        return 0;


}
