/* This program uses scanf to read the characters and print the characters on the screen. */

#include <stdio.h>

int main()
{
      char c1, c2; // Two variables which can store any character
      scanf("%c%c", &c1, &c2);  // Reading the characters
      printf("%c%c", c1, c2);   // Print the characters
      return 0;
}
