#include<stdio.h>

int main()
{
      //Two variables to store any character
      char c1, c2; 
      //Read any two characters
      scanf("%c %c", &c1, &c2); 
      //Print the characters
      printf("%c %c", c1, c2); 
      return 0;
}
