#include<stdio.h> //includes standard input output library

int main()
{
	char c1, c2;// creates variables in memory
	printf("Type 2 characters : "); //prints as instructed
	scanf("%c%c", &c1, &c2); //takes input from user
	printf("%c%c", c1, c2); // prints the input
	return 0;
}
	
