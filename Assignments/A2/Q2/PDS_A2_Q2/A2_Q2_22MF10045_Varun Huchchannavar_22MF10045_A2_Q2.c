/* This program reads two characters using scanf and the characters on the screen.*/
 #include<stdio.h>

int main()
  {
            int a;
            char c;
            float f;
              //char c1,c2;
             scanf("%d %c %f" ,&a,&c,&f); // Read any two characters
            printf("%d %c %f", a,c,f); // Print the characters
 return 0;
}
