#include<stdio.h>
int main()// main function
{
   int i; // declaring integer
   char c;// declaring character
   float f; // declaring float
   scanf("%2d%c%f", &i, &c, &f);// accepting values
   printf("%2d%d%f", i, c, f);// printing values on screen
   return 0;
}
