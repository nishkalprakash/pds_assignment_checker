#include <stdio.h>
#include <math.h>
int main()
{
	float a=.306,s=1.2;
	double tp,rs=6.96e8,ts=1.3654e-11,d=1.496e11;
	tp=ts*sqrt((rs*sqrt((1-a)/s))/2*d));
	printf("tp is %f",tp);
	scanf("enter rs %le", &rs);
	scanf("enter d %le", &d);
	scanf("enter ts %le", &ts);
	scanf("enter a %f ",&a);
	scanf("enter s %f ",&s);
	tp=ts*sqrt(rs sqrt((1-a)/s))/2*d);
	printf("tp is %f", tp);
	
	
	
	
	
	
	return 0;
}	
	
