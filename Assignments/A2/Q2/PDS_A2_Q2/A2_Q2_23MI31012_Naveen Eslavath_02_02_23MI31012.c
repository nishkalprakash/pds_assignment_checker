#include <stdio.h>
int main()
{
  int a,x,y;
  scanf("%d",&a);
  x=a/12;
  y=x+1;
  if ("a%12==0\n")
     printf("The person is=%d year old\n",x);
  else 
     printf("the person is=%d year old\n",y);
  return 0;
}
  
  
