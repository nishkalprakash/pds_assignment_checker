#include<stdio.h>

#include<math.h>

int sum(int n);

float mean(int *a , int n)

{
    int n,*a[n],i;

    printf("enter no of elements:");

    scanf("%d",&n);

    printf("Enter %d elements:",n);

    for (i=0;i<=n;i++)

    {
        scanf("%d",&*a[i]);
    }

    printf("sum of %d elements:",sum);

    for (i=0; i<=n,sum=0, a[i]+=a[i];i++)/*sum=>a[i]+=a[i]*/

    {
        scanf("%d",&sum);
    }

   int main()
{

    int n,a[n],i;

    printf("The value of *a[%d] is = %d\n, ",i,*a[i]);

    int sum(int n);

     printf("sum of %d elements:",sum);

    float mean(int *a , int n);

    printf("mean is %d",mean);

}

   int sum(int n);

   sum=>a[i]+=a[i];

   float mean(int *a , int n);

   mean=(sum)/n;

   {
       return mean;
   }

}
