//roll no.:<23AG10027>
//Name:<Komali nagendla>
//SET B//
#include<stdio.h>
#include<stdlib.h>
int main()
{
  int arr[50];
  int i,max==0,min==0,sum==0,maxcount==0;
  float avg;
  for(i=0;i<50;i++)
  printf("array\n",(rand()%80)+20);
  scanf("%d\n",&arr[50]);
  for(i=0;i<50;i++)
    arr[i]>max;
  max=arr[i]
  printf("enter the max value is %d",max);
  for(i=0;i<50;i++)
    min>arr[i];
  min=arr[i]
  printf("enter the min value is %d",min);
  for(i=0;i<50;i++)
    sum+=arr[i];
  printf("enter the sum is %d",sum);
  for(i=0;i<50;i++)
    avg=sum/50;
  printf("Enter the average is %f",avg);
  duplicate=arr[i]
  for(i=0;i<50;i++)
    duplicate++,arr[50];
  
  printf("%d\n",duplicate);
  arr[i]<arr[i+i];
  count ++;
  count>maxcount;
  maxcount=count;
  for(i=0;i<50;i++)
  printf("enter the count is %d",maxcount);
  for(i=0;i<50;i++)
  printf("array\n",arr[50]);
  for(i=0;i<50;i++)
    arr[i]>arr[i+1];
  count++;
  count>maxcount;
  maxcount=count;
  printf("enter the count is %d",maxcount);
  for(i=0;i<50;i++)
  printf(" array\n",arr[i]);
  arr[i]=(arr[i]-duplicate)/3;
  printf("%d\n",arr[i]);
  return 0;
}
