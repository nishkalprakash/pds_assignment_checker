#include <stdio.h>
/*
Name : Vankudoth Pavan
Department : Chemical Engineering
Roll No : 21CH10075
Package : Code blocks
Operating system : Windows
*/
int maxAlphaValue(int S[])
{
    int maxalpha=0,alpha,i,j;
    for (i=0;i<S[-1];i++){
        alpha=0;
        for (j=0;j<i;j++){
            if ((S[j])%(S[i])==0) alpha++;
        }
        if (alpha>maxalpha) maxalpha=alpha;
    }
    return maxalpha;
}

int main()
{
    int i,n;
    printf("n : ");
    scanf("%d",&n);
    int ints[n+1];
    printf("Enter array S[] : ");
    for (i=0;i<n;i++) scanf("%d",ints+i);
    ints[-1]=n;
    printf("Output : %d",maxAlphaValue(ints));
    return 0;
}
