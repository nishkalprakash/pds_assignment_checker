#include <stdio.h>
#include <stdlib.h>
int minTotalIqDiff(int m, int[ ] n,int N)
{
}
int main()
{
    int n,m;
    printf("Enter number of students: ");
    scanf("%d", &n);
    int arr[n];
    for (int i = 0; i < n; i++)
        scanf("%d", &arr[i]);
    printf("Enter m : ");
    scanf("%d", &m);
}
