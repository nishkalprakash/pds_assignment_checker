#include<stdio.h>
#include<math.h>
int main()
    {
     int n,count;
     if(n==0&&n<=100000)
     scanf("%d",&n);
     printf("n=%d",n);
     int digits(int n)
     {
     if(n==0);
     count=1+digits(n/1);
     return count;
     } 
     int IsPalindrome(int n)
     {
     if(n%10==n/(int)pow(10,count-1))
      printf("Palindrome");
       else
      printf("Not Palindrome");
     }
    }
    
    
   
