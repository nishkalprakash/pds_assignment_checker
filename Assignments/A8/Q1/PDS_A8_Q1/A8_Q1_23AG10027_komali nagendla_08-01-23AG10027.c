#include<stdio.h>
int main()
{
  int arr[100][100],count=0,x,y,n,s;
  
  for(x=0;x<100;x++)
    {
      for(y=0;y<100;y++)
	{
	printf("%d\n",rand()%100);
      printf("display the array",arr[100][100]);
      scanf("%d\n",&arr[100][100]);
    }
}
      for(x=0;x<100;x++)
	{
	  for(y=0;y<100;y++)
	    {
	      if(letter>=1 && letter<=2)
		printf("enter the words",letters(1-2));
	      else if(letter>=3 && letter<=5)
		printf("enter the words ",letter(3-5));
	      else(letter>5)
		    printf("enter the words"letter>5);
	    }
	}
      for(x=0;x<100;x++)
	{
	  for(y=0;y<100;y++)
	    {
	      if (arr[x][y]=a)
		count++;
	      printf("no.of a's is : %d",count);
	      if (arr[x][y]=e)
		count++;
	      pintf("no.of e's is :%d",count);
	      if(arr[x][y]=i)
		count++;
	      printf("no.of i's is:%d",count);
	      if(arr[x][y]=o)
		count++;
	      printf("no.of o's is:%d",count);
	      if(arr[x][y]=u)
		count++;
	      printf("no.of u's is:%d",count);
	      if (arr[x][y]=lab)
		printf("display the address of arr[i][j] is:%d");
	      if (no.of lab's >1)
		count++;
	    }
	}
	      printf("no.of lab's is : %d",count);
	      duplicate=(total-repeated word)/3
		arr[n][s]=arr[x][y]-duplicate
		printf("%d\n",arr[n][s]);
	      for(x=0;x<100;x++)
		{
		  for(x=0;x<100;x++)
		    {
		      if arr[x][y]=lab
			      printf("arr[x][y]=class",arr[i][j]);
		      if no.of lab's>1
			      printf("only replace lab with least (x&&y)");
		    }
		}
	      printf("enter the 2D array ",arr[x][y]);
	      printf("enter the length of each string is:%d");
	      for(x=0;x<100;x++)
		{
		  for(y=0;y<100;y++)
		    {
	  length of arr[x+1][y+1]>arr[x][y]
		arr[x][y]=arr[x+1][y+1]
	    printf("enter a[x][y] is :%d%d");
		    }
		}
	      printf("%d%d\n",a[x][y]);
	      return 0;
}
	      
		
		
		    
