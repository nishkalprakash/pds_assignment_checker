//Section 2
//RollNo:22ME30031
//Name:Jigyanshu Pati
// LAB 8
//description:Q2
#include<stdio.h>
struct matrix
{
   int m;
   int n;
   int **ptr;
}a,b,c;
struct multi mult (struct multi a,struct multi b)
{
    int i,j
    for(i=0;i<a,m;i++)
    {
        for(j=0;j<b.n;j++)
        {
            c.ptr[i][j]=a.ptr[i][j]*b.ptr[i][j]
        }
    }
}
int main()
{
    int i;
    printf("Enter the number for(j=0;j<a.n;j++)
        {
             scanf("%d",&a.ptr[i][j]);
        }of rows of matrix A:")
    scanf("%d",a.m);
    printf("Enter the number of columns of matrix A:")
    scanf("%d",a.n);
    printf("Enter the number of rows of matrix B:")
    scanf("%d",b.m);
    printf("Enter the number of columns of matrix B:")
    scanf("%d",b.n);
    a.ptr=(int**)malloc (a.m*sizeof(int *))
    for (i=0;i<a.m;i++)
    {
        ptr[i]=(int *)malloc(a.n*sizeof(int));
        printf("Enter the values of matrix A :\n")
        for(j=0;j<a.n;j++)
        {
             scanf("%d",&a.ptr[i][j]);
        }
    }
    b.ptr=(int**)malloc (b.m*sizeof(int *))
    for (i=0;i<b.m;i++)
    {
        ptr[i]=(int *)mal//Section 2
//RollNo:22ME30031
//Name:Jigyanshu Pati
// LAB 8
//description:Q2
#include<stdio.h>
struct matrix
{
   int m;struct multi mult (struct multi a,struct multi b)
{
    int i,j
    for(i=0;i<a,m;i++)
    {
        for(j=0;j<b.n;j++)
        {
            c.ptr[i][j]=a.ptr[i][j]*b.ptr[i][j]
        }
    }
}
   int n;
   int **ptr;
}a,b,c;
int main()
{
    int i;
    printf("Enter the number for(j=0;j<a.n;j++)
        {
             scanf("%d",&a.ptr[i][j]);
        }of rows of matrix A:")
    scanf("%d",a.m);
    printf("Enter the number of columns of matrix A:")
    scanf("%d",a.n);
    printf("Enter the number of rows of matrix B:")
    scanf("%d",b.m);
    printf("Enter the number of columns of matrix B:")
    scanf("%d",b.n);
    a.ptr=(int**)malloc (a.m*sizeof(int *))
    for (i=0;i<a.m;i++)
    {
        ptr[i]=(int *)malloc(a.n*sizeof(int));
        printf("Enter the values of matrix A :\n")
        for(j=0;j<a.nstruct multi mult (struct multi a,struct multi b)
{
    int i,j
    for(i=0;i<a,m;i++)
    {
        for(j=0;j<b.n;j++)
        {
            c.ptr[i][j]=a.ptr[i][j]*b.ptr[i][j]
        }
    }
};j++)
        {
             scanf("%d",&a.ptr[i][j]);
        }
    }
    b.ptr=(int**)malloc (b.m*sizeof(int *))
    for (i=0;i<b.m;i++)
    {
        ptr[i]=(int *)malloc(b.n*sizeof(int));
        printf("Enter the values of matrix B :\n")
        for(j=0;j<b.n;j++)
        {
             scanf("%d",&b.ptr[i][j]);
        }
    }
    printf("Matrix before multiplication:\n");
    printf("Matrix A:\n")
     for (i=0;i<a.m;i++)
        {
            for(j=0;j<a.n;j++)
        {
             scanf("%d ",&a.ptr[i][j]);
        }
        printf("\n");
        }
        printf("Matrix B:\n")
     for (i=0;i<b.m;i++)
        {
            for(j=0;j<b.n;j++)
        {
             scanf("%d ",&b.ptr[i][j]);
        }
        printf("\n");
        }

    if (a.n!=b.m)
    {
        printf("Error: Invalid matrix sizes for multiplication!");
    }
    else
    {
       
    

    }
}loc(b.n*sizeof(int));
        printf("Enter the values of matrix B :\n")
        for(j=0;j<b.n;j++)
        {
             scanf("%d",&b.ptr[i][j]);
        }
    }
    printf("Matrix before multiplication:\n");
    printf("Matrix A:\n")
     for (i=0;i<a.m;i++)
        {
            for(j=0;j<a.n;j++)
        {
             scanf("%d ",&a.ptr[i][j]);
        }
        printf("\n");
        }
        printf("Matrix B:\n")
     for (i=0;i<b.m;i++)
        {
            for(j=0;j<b.n;j++)
        {
             scanf("%d ",&b.ptr[i][j]);
        }
        printf("\n");
        }

    if (a.n!=b.m)
    {
        printf("Error: Invalid matrix sizes for multiplication!");
    }
    else
    {
       
    

    }
}