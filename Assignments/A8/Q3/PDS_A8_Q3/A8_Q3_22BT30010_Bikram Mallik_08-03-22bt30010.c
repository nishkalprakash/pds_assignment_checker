#include<stdio.h>
struct student {
char rollno[10];
float marks[3];
float total;
};
int main()
{
    int i,n;
    char a[i];
    struct student st[n];
    printf("enter the records for n no of of students\n");

    for(i=0;i<n;i++)
    {
       printf("enter roll nos:");
       scanf("%s",&a[i]);
    }
}
