#include<stdio.h>		//standerd io header file
#include<math.h>		//math library
int main(){
	int x; 				//x is an integer number
	int p,q;
	int p,q,r;
	int p,q,r,s;
	int p,q,r,s,t;
	int p,q,r,s,t,u;

	printf("enter the integer number:");

	scanf("%d",&x);		// enter the value of x

   
   if(x<100){			// if else statement
  	int x=pq;
  	int y=pow(p,2)+pow(q,2);
  	printf("%d\n",y );
  	break;

   } 
    else if(x<1000){			// if else statement
  	int x=pqr;
  	int y=pow(p,3)+pow(q,3)+pow(r,3);
  	printf("%d\n",y);
	break;

   } else if(x<10000){			// if else statement
  	int x=pqrs;
  	int y=pow(p,4)+pow(q,4)+pow(r,4)+pow(s,4);
  	printf("%d\n",y );

  	break;

   } else if(x<100000){			// if else statement
  	int x=pqrst;
  	int y=pow(p,5)+pow(q,5)+pow(r,5)+pow(s,5)+pow(t,5);
  	printf("%d\n",y );

  	break;

   } else if (x<1000000){			// if else statement
  	int x=pqrstu;
  	int y=pow(p,6)+pow(q,6)+_pow(p,6)+pow(q,6)+pow(t,6)+pow(u,6);
  	printf("%d\n",y );
  	break;

    } default(x<=0){                    //if else statement

	printf("invalid input");

	}

   	return 0;           // the code ends here
	}